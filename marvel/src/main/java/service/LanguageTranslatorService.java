package service;

import models.Character;

import java.util.Locale;

public class LanguageTranslatorService {
    public Character translateInto(String input, String languageCode) {
        Locale locale = new Locale(languageCode);
        return  null;
    }
}
